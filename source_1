def source_1(spaceP,t):
    
    sourceP = [SO]
    QS = np.zeros(N)
    Sfac = np.ones(N)
    
    return QS,Sfac,sourceP
    
--------------------example

import numpy as np
spaceP = 0.5
t = 1.0
N = 4
S0 = 0
source_1(spaceP,t)
