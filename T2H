def T2H(T, Cpave):
    Tref = 0
    H = Cpave * (T - Tref)
# Converts temperature to enthalpy.
# The specific heat is allowed to be temperature dependent
# The reference temperature is assumed to be O C.
    return H
#--------------------example
import numpy as np
T = 25.0
Cpave = np.array([0.5, 0.8,0.9,1.0])
T2H(T,Cpave)
