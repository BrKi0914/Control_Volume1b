mport os
from win32api import GetSystemMetrics
GetSystemMetrics(0)
GetSystemMetrics(1)
scn = [1,1,GetSystemMetrics(0),GetSystemMetrics(1)]
if scn[2] > 1500:
    print('')
    print(' [0] full screen')
    print(' [1] for PPT (1/4 page)')
    print(' [2] for PPT (full page)')
    print(' [3] for manuscript (2-column format)')
    print(' [4] for manuscript (full page width)')
    Ssize = input('Type [0,1,2,3,4]: ')
    pos = set_screen(Ssize)
else:
    pos = [480, 40, 1090, 800]
f = 0.8
jflag = 1

# the code below comes from the set_experim code too
experiment = set_experim
region = experiment[0]
project = experiment[1]
experim = experiment[2]


# change working directory and add temporary paths

# experim comes from the set_experim code 
fid = open('_namelist.txt','r')
header = fid.readline()
blank = fid.readline()
Lname_s = fid.readline
Tsname_s = fid.readline()
Pname_s = fid.readline()
Sname_s = fid.readline()
status = fid.close()

# extract_str is a code that I converted from MATLAB to Python
# extracts the comment from the line
# for example:  Lname = extract_str(Lname_s) is e1_layer since Lname_s is el_layer %layers 
Lname = extract_str(Lname_s)
Tsname = extract_str(Tsname_s)
Pname = extract_str(Pname_s)
Sname = extract_str(Sname_s)
