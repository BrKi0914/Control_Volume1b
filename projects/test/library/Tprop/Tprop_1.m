 function [K,rho,Cp,C,diagC] = Tprop_1(~,materialC,~)% Thermal properties at CV grid points.% Tprop version '1' (test).% 2 simple materials, no rho- or T-dependence.% unpack material parameters typ = materialC{1};% initializations K   = zeros(size(typ)); rho = zeros(size(typ)); Cp  = zeros(size(typ));% Type 1 material ------- L = typ == 1; rho(L) = 2000; Cp(L)  = 1000; K(L)   = 2;% Type 2 material ------- L = typ == 2; rho(L) = 2500; Cp(L)  = 1600; K(L)   = 4;% bulk heat capacity C = rho .* Cp;% temperature weighted specific heat Cpave = Cp;% store Cpave in diagnostics cell array diagC = {Cpave};