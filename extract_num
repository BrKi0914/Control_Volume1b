def extract_num (s):
# Extract a number from a line that contains a comment.
# Eg.     -10.5   # surface temperature
    k=str1.find("%")
    x=float(str1[0:k])
    return x

#example for extract_num
#str1 = "-10.5   % surface temperature";
#extract_num (str1)    
